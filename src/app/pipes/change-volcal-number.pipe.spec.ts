import { ChangeVolcalNumberPipe } from './change-volcal-number.pipe';

describe('ChangeVolcalNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeVolcalNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
